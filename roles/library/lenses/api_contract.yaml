name: api_contract
purpose: Keep public interfaces, input/output contracts, and compatibility boundaries
  stable.
allowed_tools:
- code_index
- code_find_symbol
- code_dependency_graph
- fs_read
forbidden_tools:
- fs_write
- fs_str_replace
- terminal_run
output_schema:
  lens: api_contract
  contracts: list
  breaking_changes: list
