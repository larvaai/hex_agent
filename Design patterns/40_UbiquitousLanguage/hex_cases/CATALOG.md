# CATALOG — mọi occurrence của Ubiquitous Language trong hex_agent

> Vét cạn từ bước discover. Mỗi `path:line` đã được MỞ FILE xác minh khi dựng tài liệu này.
> Path tương đối tính từ root `hex_agent/`. Độ rõ = mức độ thể hiện pattern rõ tới đâu.

| path:line | Mô tả (vai trò trong Ubiquitous Language) | Độ rõ |
|---|---|---|
| [`docs/GLOSSARY.md:1-19`](../../../docs/GLOSSARY.md) | Bảng glossary chính: 9 term load-bearing (chokepoint, roster-growth, department alias, to-admit member, safe checkpoint, pending command, command bridge, trust-O, authority gate, `attribution≠authz`, fail_open, passed_with_risk). Mỗi term có nghĩa + trỏ `file:line`. Đây là *single source of truth* của UL. | cao |
| [`harness/tests/test_glossary_invariants.py:1-69`](../../../harness/tests/test_glossary_invariants.py) | CI gate canh glossary: file phải tồn tại, bảng phải còn cột Term/Definition/Forbidden/Backing, các core term phải còn hàng, ban-wording phải đăng ký. Chặn glossary bị rỗng hoá/drift. | cao |
| [`docs/decisions.md:25-27`](../../../docs/decisions.md) | DEC-2 dùng UL "roster-growth" + "department" + "authority gate" trong quyết định kiến trúc E21; nối term với spec slice (S10.x) và file:line. Rename term → phải update ADR. | cao |
| [`docs/decisions.md:103-105`](../../../docs/decisions.md) | DEC-8 doctrine `attribution≠authz`: `issued_by`/Actor = ghi nhận (audit), authz thật = `requires_permission` + checkpoint. Phân biệt ngữ nghĩa cross-context được codified thành nguyên tắc quyết định. | cao |
| [`supervisor/graph.py:1-8`](../../../supervisor/graph.py) | Docstring module: "Supervisor nodes — compose_team / o_decide / run_round / judge / tool. Epic E10." Nối code với epic chiến lược; tên node = tên phase nghiệp vụ. | cao |
| [`supervisor/graph.py:82-104`](../../../supervisor/graph.py) | `compose_team()` (S10.1) phát `emit("loop.team_composed", ...)`; error "Team composition selected unknown agents" lặp đúng domain term, không đổi tên. | cao |
| [`supervisor/graph.py:107-123`](../../../supervisor/graph.py) | `o_decide()` (S10.8) phát `loop.decision` + `loop.parse_error` — tên event theo domain (O decision phase), không phải `orchestrator_output`/`json_parse_fail`. | cao |
| [`supervisor/graph.py:136-211`](../../../supervisor/graph.py) | `run_round()` (S10.2/3/5/14): "authority check" — mọi assignment phải target agent đã được composition chọn (authority gate, dòng 142-149). Comment lặp domain concept, không rename. | cao |
| [`delegation/manager.py:1-2`](../../../delegation/manager.py) | Docstring: "Sequential delegation chokepoint" — term load-bearing từ GLOSSARY.md. Delegation là MỘT entry point duy nhất, ép guardrail. | cao |
| [`delegation/manager.py:45-61`](../../../delegation/manager.py) | `_finish()` publish `delegation.finished` kèm domain context: `outcome`, `artifact_count`, `error`. Không phải `job_done`/`execution_complete`. | cao |
| [`delegation/manager.py:91-117`](../../../delegation/manager.py) | publish `delegation.started`; trong `delegate()` các verb domain: validate → start → resolve → run. Event lifecycle theo UL nghiệp vụ. | cao |
| [`control/events.py:1-11`](../../../control/events.py) | Docstring `RuntimeEvent` envelope (Epic E21, S21.1): "the single shape every control-plane event uses". Một shape cho UI/audit/replay. Không phải `LogEntry`. | cao |
| [`control/events.py:23-25`](../../../control/events.py) | `ACTOR_TYPES = {human, agent, tool, system, runtime}` + `VISIBILITY_LEVELS = {public, ui_safe, internal, secret, restricted}` — vocabulary metadata theo domain, không phải `debug/info/warn`. | cao |
| [`control/events.py:32-50`](../../../control/events.py) | `Actor` dataclass: `__post_init__` ép `type ∈ ACTOR_TYPES`, sai → `ControlContractError`. UL được enforce bằng validation. | cao |
| [`roles/spec.py:1-50`](../../../roles/spec.py) | `RoleSpec` (canonical) + `RoleView` (E10 projection). Field: name, role, department, system_prompt, allowed_skills, may_route_to, test_ownership. "department" là domain concept (không phải `group_id`); "may_route_to" là UL domain. | cao |
| [`supervisor/contracts.py:20-75`](../../../supervisor/contracts.py) | `SessionPlan`, `AgentSelection`, `AgentAssignment`, `OrchestratorDecision`, `ContextPacket` — 5 cấu trúc dữ liệu domain cho orchestrator BC. Tránh jargon: `objective` (không `goal_id`), `scope_of_work` (không `permission_mask`), `briefing` (không `context_json`). | cao |
| [`core/schemas.py:132-178`](../../../core/schemas.py) | `DelegationSpec`, `DelegationPolicy` (+ `DelegationProgress`/`Request`/`Result` ở quanh đây) — object domain của delegation BC. Quy ước đặt tên `Delegation*` nhất quán, chỉ dùng trong module delegation + test. Không rò ngữ nghĩa. | cao |
| [`delegation/policy.py:1-32`](../../../delegation/policy.py) | `DelegationPolicyEngine` ép ràng buộc delegation. Error "Delegation depth limit exceeded" dùng đúng term glossary; thông báo lỗi nói ngôn ngữ domain. | cao |
| [`supervisor/broker.py:1-55`](../../../supervisor/broker.py) | `DeterministicBroker.write_packet()`: `AgentAssignment.objective` + `scope_of_work` → `ContextPacket.briefing`. Tái dùng term domain, không jargon dịch thuật. | cao |
| [`control/command_registry.py:1-60`](../../../control/command_registry.py) | `CommandTypeRegistry` (S21.4): mỗi command_type khai `apply_at` + `requires_permission`. Term `requires_permission` được DEC-8/doctrine trỏ tới như "authz thật". | cao |
| [`config/runtime_command_types.yaml:9-37`](../../../config/runtime_command_types.yaml) | Catalog command: tên theo domain — `AddAgentToLoop` (không `PushAgent`), `UpdateAgentPermission` (không `SetACL`), `RemoveAgentFromLoop`, `EditAgentInstruction`. Mỗi cái khai `requires_permission`. | cao |
| [`core/kernel.py:76-106`](../../../core/kernel.py) | `AgentKernel.execute_tool` — "the single execute_tool chokepoint" (docstring dòng 82). Dùng đúng term "chokepoint" của glossary, không phải `dispatcher`/`gateway`/`execute`. (Lưu ý: glossary trỏ :63 nhưng `def execute_tool` thật ở dòng 106.) | cao |
| [`docs/explanation/authz-vs-attribution.md:1-57`](../../../docs/explanation/authz-vs-attribution.md) | Doctrine giải thích phân biệt attribution (`issued_by`, tự khai) vs authz (`requires_permission`, checkpoint). Cùng term glossary `attribution≠authz` + DEC-8 tạo định nghĩa UL bền. | cao |
| [`harness/scripts/check_report_language.py:1-50`](../../../harness/scripts/check_report_language.py) | Tool canh tính nhất quán ngôn ngữ output (EN vs VI) + AI-tells. Không trực tiếp là UL nhưng thể hiện kỷ luật giữ "ngôn ngữ thuần" — chống comment trôi sang dịch máy. | trung bình |
| [`harness/tests/test_bug_class_invariants.py:1-55`](../../../harness/tests/test_bug_class_invariants.py) | Invariant chống bug-class: ban wording "write-fence" cho fs_guard (tên không được hứa điều cơ chế không làm được). Test canh tuân thủ glossary ở quy mô lớn. | trung bình |
| [`MAP.md:1-40`](../../../MAP.md) | Module map tự sinh. Mô tả dùng term nhất quán: "chokepoint", "Blackboard", "session", "round_no". Không phải "entry point"/"state object"/"task". | trung bình |
